"Commands"
{
	"!rr"
	{
		"cmd" "rr" //  sv command to use
		"flags" "bc" // admin flags required for use this command.
	}
	"!start"
	{
		"cmd" "esl5on5" // sv command to use
		"flags" "bc" // admin flags required for use this command.
	}
	"!knife"
	{
		"cmd" "knife" // sv command to use
		"flags" "bc" // admin flags required for use this command.
	}
	"!pracc"
	{
		"cmd" "pracc" // sv command to use
		"flags" "bc" // admin flags required for use this command.
	}
	"!praccoff"
	{
		"cmd" "praccoff" // sv command to use
		"flags" "bc" // admin flags required for use this command.
	}
}

// btw you can USE / of command.
// Example: /start or /pracc
